# 힙

```python
import heapq

heap = []
heapq.heappush(heap, 원소); heapq.heappop(heap, 원소)
```

## 더 맵게

- 모든 원소값이 k 이상인가 == 컨테이너 최솟값이 k 이상인가 -> 어떤 조건을 그대로 옮기기보다 같은 논리로 더 간단하게 할 수 있을까를 의식적으로 고민하기

## 디스크 컨트롤러

- 힙 원소가 튜플 형식이면 인덱스 순서로 값을 비교한다.
  - 만약 다중 조건이며 동시에 큰 것 기준도 있으면? ++**큰거는 -1을 곱해서 최소힙에 강제로 맞춰둔다.**++
- 요청 시간 기준으로 미리 정렬하고 cur 포인터를 두면 job[cur].req_time <= time; cur+=1 으로 처리할 수 있다.
- heap 알고리즘 짤 땐 ++**반드시 heap.empty()? 인 엣지 상황도 고려**++

